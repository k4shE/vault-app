apiVersion: argoproj.io/v1vault1
kind: Application
metadata:
  name: vault-app
  namespace: argocd
spec:
  project: default

  source:
    - repoURL: https://github.com/k4shE/vault-app.git
      targetRevision: HEAD
      path: vault
  destination:
    server: https://kubernetes.default.svc
    namespace: vault

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
